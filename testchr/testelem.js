CHR.Modules.TestElem = {

	version:'0.0.1',

  	testCHRCode: function () {

        // let outputContainer = document.getElementById('outputContainer');
        // let codeselect = document.getElementById('codeselect');



        // let test = function() { resolve.Test.d('ok'); };

        // Gui.ttt1 >>> resolve.Test.b;

        let test;

        ({a,b},- {type:'type'}) >>> a === 'b' | {a:'a'}, console.log('x',x);

        // ttt2 >>> resolve.c('ok');
        // ttt3 >>> (
        //     cf = function() { resolve.Test.d('ok'); }
        // );

        // test(a,b) >>> document[a]('ok',b);
        // test(a,b) >>> a[document]('ok',b);

        // let onclick;

        // - notifyCancel(c,fundo) >>> (
        //     onclick = function() { addClass('','canceled'); fundo(); }
        // );

        // - delayed(delay,action,args) >>> (
        //     tid = window.setTimeout( function() { resolve[action].apply(null,args); }, delay),
        //     // log('stopTimer/3', tid),
        //     delayId(tid)
        // );

        // l(a) >>> console.log('l',a);
        // l(a,b) >>> console.log('l',a,b);
        // l(a,b,c) >>> console.log('l',a,b,c);
        // l2(a,b,c) >>> console.log('l2',a,b,c);

        // invoke(c) >>> 
        //     chr.call(c,[2,3]), 
        //     chr.call(c,[4,5]), 
        //     chr.call(c,modname + '.l2',[5,6]);

        // - init >>> invoke(l(1));
    } 
};
